{%- comment -%}
  Star rating — renders filled stars.
  Usage: {% render 'star-rating', stars: 5 %}
{%- endcomment -%}

{%- assign star_count = stars | default: 5 -%}
<span class="stars" aria-label="{{ star_count }} out of 5 stars">
  {%- for i in (1..star_count) -%}★{%- endfor -%}
</span>
