{%- comment -%}
  Exit intent — triggers the ShopLeadPopup when mouse leaves viewport.
  JS logic handled in theme.js (initExitIntent).
  This snippet is intentionally empty — the logic fires open-discount-popup
  which is handled by the shop-lead-popup snippet.
{%- endcomment -%}
