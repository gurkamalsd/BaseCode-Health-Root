<section class="section bg-white">
  <div class="container container--narrow">
    <h1 class="text-center mb-8">{{ page.title }}</h1>
    <div class="rte" style="font-size: var(--text-base); line-height: 1.8; color: var(--color-text-light);">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Page Content",
  "settings": []
}
{% endschema %}
